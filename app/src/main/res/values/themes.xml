<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HisarApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/accent</item> -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="colorOnPrimary">@color/accent</item>
        <item name="colorSurface" >@color/white</item>
        <item name="colorAccent">@color/accent</item>

    </style>

    <style name="Theme.HisarApp" parent="Base.Theme.HisarApp" />

    <style name="MyDatePickerStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="textAppearanceButton">@color/accent</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:colorSecondary">@color/error</item>
        <item name="android:textColor">@color/accent</item>
        <!--selected Item color-->
        <item name="colorControlActivated">@color/accent</item>
        <!-- Highlight item color-->
        <item name="colorControlHighlight">@color/accent</item>
    </style>

</resources>